<div class="row mb-4">
  <div class="col-md-6 offset-md-3">
    <h1 class="display-4">
      discover

    </h1>
    
    <p class="lead text-muted">posts liked by people you follow</p>
  </div>
</div>

<% @user.discover.latest.each do |photo| %>
  <%= render "photos/photo", photo: photo %>
<% end %>
